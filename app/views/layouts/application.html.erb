<!DOCTYPE html>
<html>
  <head>
    <title>EventShoppingListCreator</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <% if logged_in? %>
      <%= render 'layouts/sidebar' %>
    <% end %>
    <%= yield %>
  </body>
</html>
